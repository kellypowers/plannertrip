<%if flash[:message].present?%>
    <%=flash[:message]%>
<%end%>

Event Name <%=@event.name%>
<br>

Event Category<%=@event.category%> <br>
Event Description <%=@event.description%><br>
Event Address <%=@event.full_address%>

<%form_for current_user do |f|%>
  <%=f.collection_check_boxes, :planner_ids, current_user.planners, :id, :name %>
 <%=f.submit "Add this event to a planner"%>
 <br>
 <%=link_to "Edit this Event", edit_event_path(@event) %>
<br>
<%= form_tag event_path(@event.id), method: "delete" do %>
  <%= submit_tag "Delete #{@event.name}" %>
<% end %>


