<div>
    <h1> Find the distance between two of your events </h1>
</div>

<div>
    <h2>Event 1: </h2>
    <%form_for @user do |f|%>
    
    <%= f.collection_select :events, @user.events, :id, :name %>
    <%end%>

</div>

<div>
    <h2>Event 2: </h2>
    <%form_for @user do |f|%>
    
    <%= f.collection_select :events, @user.events, :id, :name %>
    <%end%>

</div>